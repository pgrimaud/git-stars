{% extends 'base.html.twig' %}

{% block body %}
    {% if app.user %}
        <p>Hello {{ app.user.username }}</p>
        <a href="{{ path('security_logout') }}">Logout</a>
    {% else %}
        <a href="{{ hwi_oauth_login_url('github') }}">Login with GitHub</a>
    {% endif %}
{% endblock %}